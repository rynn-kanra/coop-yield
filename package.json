{
  "name": "coop-yield",
  "version": "1.0.3",
  "type": "module",
  "main": "dist/coop-yield.js",
  "module": "dist/coop-yield.js",
  "types": "dist/coop-yield.d.ts",
  "scripts": {
    "build": "bun run build:min.js && bun run build:js && bun run build:type",
    "build:js": "bun build ./src/coop-yield.ts --outdir=dist/ --sourcemap=linked --target=node --format=esm",
    "build:min.js": "bun build ./src/coop-yield.ts --outdir=dist/ --sourcemap=linked --target=node --format=esm --minify --entry-naming=[dir]/[name].min.js",
    "build:type": "bunx tsc --emitDeclarationOnly --skipLibCheck --outDir dist/",
    "test": "bun run test:unit && bun run test:fmt",
    "test:unit": "bun test ./test",
    "test:fmt": "prettier --check './*.{mjs,js,ts,md,json,html}' './{src,docs,tests}/{,**/}*.{mjs,js,ts,md,json,html}'",
    "fmt": "prettier --write './*.{mjs,js,ts,md,json,html}' './{src,docs,tests}/{,**/}*.{mjs,js,ts,md,json,html}'"
  },
  "author": {
    "name": "rynn-kanra",
    "email": "blackhaze879@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rynn-kanra/coop-yield.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/wicg-task-scheduling": "^2024.1.0",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
